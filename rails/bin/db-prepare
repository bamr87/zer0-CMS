#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../config/environment"
require "fileutils"

FileUtils.mkdir_p(Rails.root.join("storage"))
ActiveRecord::Base.connection
schema = Rails.root.join("db/schema.rb")
unless ActiveRecord::Base.connection.table_exists?(:sites)
  load schema
end
