# The draft queue

Markdown files in this folder are queue items. This one is not: it carries no front matter, so `listQueue` skips it. That is the cheapest honest test for "this is prose about the queue rather than an item in it".

Statuses are `pending`, `approved` and `published`, in that order, and only a human moves an item from the first to the second.
